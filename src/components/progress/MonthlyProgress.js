import { Grid, Typography } from '@mui/material';
import React from 'react';

export default function MonthlyProgress() {
  return (
      <Grid container>
          <Typography>Montly Progress</Typography>
      </Grid>
  )
}
